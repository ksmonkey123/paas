spring:
  cloud:
    discovery:
      client:
        simple:
          instances:
            auth-service:
              - uri: http://auth-service/
            shortener-service:
              - uri: http://shortener-service/
