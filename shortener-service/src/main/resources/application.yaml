spring:
  datasource:
    url: ${DB_URL}
    username: ${DB_USER}
    password: ${DB_PASSWORD}
    hikari:
      schema: shortener
  flyway:
    default-schema: shortener
  application:
    name: shortener-service
server:
  error:
    include-message: always
paas:
  security:
    public-endpoints: /s/**
