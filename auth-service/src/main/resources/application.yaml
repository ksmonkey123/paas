spring:
  datasource:
    url: ${DB_URL}
    username: ${DB_USER}
    password: ${DB_PASSWORD}
    hikari:
      schema: auth
  flyway:
    default-schema: auth
  application:
    name: auth-service
server:
  error:
    include-message: always

paas:
  security:
    public-endpoints:
      - /login